/**
 * Summarizes the property of the entity it describes: { @see SnakData }, { @see EdgeData }, { @see SnakGroupData }, { @see ReferenceData }
 */
export interface PropertyData {
  id: string;
  label: string;
  description: string;
}
