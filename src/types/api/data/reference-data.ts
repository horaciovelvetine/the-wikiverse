import { SnakGroupData } from "./snak-group-data";

/**
 * Details the nature of the source of a { @see ClaimData } about a { @see VertexData }
 */
export interface ReferenceData {
  snakGroups: SnakGroupData;
}
