/**
 * Initial Search Result data used prior to initializing a { @see GraphsetRequest }, used in the { @see SearchRequest }
 */
export interface SearchResultData {
  entityID: string;
  pageID: number;
  title: string;
  label: string;
  description: string;
  url: string;
}
