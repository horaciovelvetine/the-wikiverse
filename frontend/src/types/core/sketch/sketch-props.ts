import { P5_SketchManager } from "./p5-sketch-manager";

/**
 * Generic Prop definition to allow passing of the the P5_Sketch to each child
 */
export interface SketchProps {
  sketchRef: P5_SketchManager;
}
